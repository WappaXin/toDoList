import { toDoListLogic } from "./module1";

export function consoleLogTest(){

    // const log = (message) => {console.log(message);}

    // const toDoListObject = toDoListLogic();

    // // addToDoItemToAll (title, description, dueDate, priority)
    // // addToDoItemToProject (arrayName ,title, description, dueDate, priority)
    // // removeToDoItem (idValue)
    // // createProject (projectName)
    // // getAllToDoItems
    // // getAllProjects
    // // removeProject(projectName)

    // toDoListObject.addToDoItemToAll( "buy groceries" , "oil,noodles,vegetables" , "today" , "medium");
    // toDoListObject.addToDoItemToAll( "buy groceries" , "oil,noodles,vegetables" , "today" , "medium");

    // log(toDoListObject.getAllToDoItems());

    // toDoListObject.createProject("study");

    // log(toDoListObject.getAllProjects());

    // toDoListObject.addToDoItemToProject( "study" , "buy groceries" , "oil,noodles,vegetables" , "today" , "medium");
    // toDoListObject.addToDoItemToProject( "study" , "buy groceries" , "oil,noodles,vegetables" , "today" , "easy");

    // log(toDoListObject.getAllProjects());

    // toDoListObject.removeToDoItem(2);

    // log(toDoListObject.getAllToDoItems());
    // log(toDoListObject.getAllProjects());

    // toDoListObject.createProject("study1");
    
    // log(toDoListObject.getAllProjects());

    // toDoListObject.removeProject("study");

    // log(toDoListObject.getAllProjects());
    // log(toDoListObject.getAllToDoItems());

    // toDoListObject.createProject("study2");

    // toDoListObject.removeProject("study1");

    // log(toDoListObject.getAllToDoItems());
    // log(toDoListObject.getAllProjects());

    // toDoListObject.removeProject("study2");

    // log(toDoListObject.getAllToDoItems());
    // log(toDoListObject.getAllProjects());

    // toDoListObject.removeToDoItem(0);
    // toDoListObject.removeToDoItem(1);

    // log(toDoListObject.getAllToDoItems());
    
}

